export interface LoginResponseType {
  accessToken: string;
  id: number;
  email: string;
  name: string;
}

export interface SignUpResponseType {
  accessToken: string;
  id: number;
  email: string;
  name: string;
}
